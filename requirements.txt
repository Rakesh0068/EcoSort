# EcoSort backend - real ML core + API
# Install PyTorch with CUDA 12.1 wheels for GPU training:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# NOTE: TensorFlow >= 2.11 has no GPU support on native Windows, so the
#       training stack here is PyTorch (CUDA verified on an RTX 3050 6GB).

torch>=2.5
torchvision>=0.20
numpy>=1.26
pillow>=10.0
opencv-python>=4.9
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
